# ── Database ──
DATABASE_URL=postgresql://user:Tct85329@pgadmin4.devapp.cc:5432/nbu_mental_health

# ── JWT ──
JWT_SECRET=change-me-to-a-random-secret
JWT_EXPIRES_IN=8h

# ── LINE Messaging API ──
LINE_CHANNEL_SECRET=your-line-channel-secret
LINE_CHANNEL_ACCESS_TOKEN=your-line-channel-access-token

# ── LINE LIFF ──
LIFF_VERIFY_ID=your-liff-verify-id
LIFF_SCREENING_ID=your-liff-screening-id
LIFF_BOOKING_ID=your-liff-booking-id

# ── LINE Rich Menu IDs ──
RICH_MENU_GUEST_ID=your-guest-rich-menu-id
RICH_MENU_VERIFIED_ID=your-verified-rich-menu-id

# ── Encryption (Clinical Notes) ──
ENCRYPTION_KEY=32-byte-hex-key-for-aes-256-gcm

# ── Server ──
PORT=3000
NODE_ENV=development

# ── Admin Portal ──
NEXT_PUBLIC_API_URL=http://localhost:3000
