#!/bin/bash
# ─────────────────────────────────────────────────────────────────────────────
# NBU Mental Health — Deploy Script
# รันบน server หลัง git pull
#
# Usage:
#   chmod +x infra/deploy.sh
#   ./infra/deploy.sh
# ─────────────────────────────────────────────────────────────────────────────

set -e  # หยุดทันทีถ้ามี error

PROJECT_DIR="/opt/nbu-mental-health"
LOG_DIR="/var/log/nbu"

echo "🚀 Starting deployment..."
cd "$PROJECT_DIR"

# ── 1. Pull latest code ──────────────────────────────────────────────────────
echo "📥 Pulling latest code..."
git pull origin main

# ── 2. Install dependencies ──────────────────────────────────────────────────
echo "📦 Installing dependencies..."
npm install --workspaces --if-present

# ── 3. Build shared package ──────────────────────────────────────────────────
echo "🔨 Building shared package..."
npm run build -w packages/shared

# ── 4. Build API ─────────────────────────────────────────────────────────────
echo "🔨 Building API..."
npm run build -w apps/api

# ── 5. Build Admin portal ────────────────────────────────────────────────────
echo "🔨 Building Admin portal..."
cd apps/admin && npm run build && cd "$PROJECT_DIR"

# ── 6. Build LIFF app ────────────────────────────────────────────────────────
echo "🔨 Building LIFF app..."
cd apps/liff && npm run build && cd "$PROJECT_DIR"

# ── 7. Create log directory ──────────────────────────────────────────────────
mkdir -p "$LOG_DIR"

# ── 8. Reload PM2 services ───────────────────────────────────────────────────
echo "♻️  Reloading PM2 services..."
if pm2 list | grep -q "nbu-api"; then
    pm2 reload ecosystem.config.js --update-env
else
    echo "▶️  Starting PM2 for the first time..."
    pm2 start ecosystem.config.js
    pm2 save
fi

# ── 9. Show status ───────────────────────────────────────────────────────────
echo ""
echo "✅ Deployment complete!"
pm2 list
